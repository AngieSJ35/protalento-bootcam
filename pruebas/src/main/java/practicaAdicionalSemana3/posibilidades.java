package practicaAdicionalSemana3;

public class posibilidades {

	public static void main(String[] args) {
		// /*Object[][] matriz = new Object[ninios][4];
		
		//for(int fila=0;fila<ninios;fila++) {
			//for(int columna=0;columna < matriz[0].length; columna++) {
				
				//matriz[fila][3]= {nombre,apellido,numExamen,notExamen}
					
				
				
				//matriz[fila][columna] = dato[j];	
	//				j++;					
		//	}i++;
			//j=0;
//		}
	//i=0;	
	//j=0;
	
	
	//extraer vector notExamen de matriz
	//Object[] notExamenMax1 = (Object[]) matriz[0][4];
	/*Se comenta para intentar usar ninio y crear el vector con los útlimos registros
	 * 
	 * 
	 * Object[] notExamen = new Object[matriz.length];
	 
	for(i = 0; i < matriz.length; i++) {
		notExamen[i]=matriz[i][3];
	}
	System.out.println(Arrays.toString(notExamen));
	i=0;
	
	Object[] notExamen1 = notExamen;
	int notaMax = (Integer) notExamen1[0];
	int notaMin = (Integer) notExamen1[0];
	//evaluamos el valor máximo
	for(i=1;i<notExamen.length;i++) {
		if ((Integer) notExamen1 [i] > notaMax) {
			notaMax= (Integer) notExamen1[i];
		}
	}
	//evaluamos el valor máximo
	for(i=1;i<notExamen.length;i++) {
		if ((Integer)notExamen1 [i] > notaMin) {
		notaMin= (Integer) notExamen1[i];
			
		}
	}*/
	//float notExamenMax1 = (float) notExamenMax;
	//float notExamenMin1 = (float) notExamenMin;
	
	//for(i=1;i<matriz.length; i++) {
		//if(matriz[i][4] > notExamenMax) {
			//notExamenMax = matriz[i][4];


	}

}
